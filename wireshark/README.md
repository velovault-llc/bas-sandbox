# Capture catalog — real-BACnet ground truth

Companion to [tools/real-bacnet-rig/README.md](../tools/real-bacnet-rig/README.md).
One line per capture: what's in it, why it matters.

| File | What | Notable |
|---|---|---|
| `lab1-first-contact.pcapng` | **First rig capture (2026-06-10).** YABE (192.168.1.150, desktop) discovers bacserv 1.4.1 device 1234 (192.168.1.165, laptop, aliased "ClaudeSimDevice"). 47 BACnet frames over ~4 min. | Full **I-Am reference encoding**: device id + Max APDU 1476 + Segmentation no-segmentation(3) + Vendor 260, NPDU global-broadcast (DNET 65535, hop 255) — the fix-template for the sandbox's abbreviated I-Am conformance ⚠. Also on the wire and **unmodeled by the sandbox**: `readPropertyMultiple` (YABE's default read), `timeSynchronization` + `utcTimeSynchronization`, `who-Has`/`i-Have`, and an **unsubscribed broadcast** `unconfirmedCOVNotification` (AV:1) — COV without any SubscribeCOV. I-Am observed ×2 per trigger ~150 µs apart (multi-send or capture artifact — unresolved). |
| `lab1-yabe-aliases.YabeMap` | YABE's local alias cache from the same session (binary .NET serialization). | Maps (device 1234 @ C0A801A5, TRENDLOG:2) → display names. Kept for session reproducibility; not device data. |
| `lab2-polling-cadence.pcapng` | **Polling-cadence fixture (2026-06-10).** Intended as the COV-lifecycle capture; YABE silently fell back to POLL mode ("Use polling by default"=True), so instead: a real client's RPM poll train at a metronomic 1.000 s with ACK latencies 3–120 ms (Wi-Fi), two clean writeProperty→Simple-ACK exchanges, and TWO device outage/recovery cycles (laptop Wi-Fi power-save — flapping, not crashing). | **bacserv boot dance**: on startup it sends `who-Is <own-range>` BEFORE i-Am — a real device checking for instance collision. Teaching material + sim candidate. Poll cadence + RTTs = tuning data for `APP_LAYER_POLL_CADENCE_S` / `BACNET_IP_RTT_SECONDS`. COV retry: unanswered poll → next attempt ~1.5 s (timeout 500 ms × 1 retry, per YABE settings). |
| `lab3-cov-lifecycle.pcapng` | **THE COV-lifecycle fixture (2026-06-10).** Full subscribe → ACK → notify → renew cycle, YABE ↔ bacserv, ~17 min incl. a YABE restart mid-capture. | The reference model extracted: (1) **renewal at exactly lifetime/2** (120 s lifetime → re-subscribe every 60 s, metronomic); (2) **initial notification on every (re)subscribe** (current value, 0.2–7 s later); (3) **change detection = \|current − last-NOTIFIED\| ≥ Cov_Increment, evaluated on a periodic SCAN** (0.2–7 s) — an intermediate write (50→60→60.1 in quick succession) was never notified at all, then 60.1-vs-50 fired; later, isolated 60→60.1 (Δ < increment 1) stayed correctly silent; (4) **duplicate subscriptions are NOT deduped** — 3 stray subscription rows = 3 notifications per change; (5) writes carry **Priority 16** (YABE default) in the WriteProperty encoding. Ghost/TTL-expiry test inconclusive (value never changed post-kill) — needs the Room Simulator's self-moving value. |
| `lab4-renewal-longevity.pcapng` | **Renewal stability at scale (2026-06-10).** ~18 min, dual-object (AV:1 + AI:1) subscriptions, renewals every 60 s, all ACKed + initial-notified — lab3's model holds long-run. | Procedural lesson baked in: Room Simulator ran on the SAME host as YABE → its whole conversation was loopback, invisible on the wire (only broadcast i-Ams from device 3358908/400001 appear). Devices must live on a different box than the client. Tail: two unACKed subscribes — the laptop flapped again at the end. |
| `lab5-roomsim-cov-ephemeral.pcapng` | **Room Simulator natural-drift COV + the ephemeral-port discovery (2026-06-10, on James's new isolated lab router, 192.168.100.0/24).** 4 subscriptions, 50+ notifications. | **THE finding of the night: both stacks silently fell back to EPHEMERAL UDP ports (61450↔63354) when 47808 was taken** — fully functional BACnet that (a) `udp port 47808` capture filters never see, (b) Wireshark won't auto-dissect (shows as anonymous "UDP" — decode-as `bvlc` required), (c) `bacapp` display filters skip. Two devices talked perfectly for HOURS while every standard-port diagnostic said the network was silent — James caught it ("I think they may be getting passed around as UDP packets"). Also: the .NET (YABE) stack's **initial notification carries the FULL object** (name/units/reliability/status-flags/…), unlike bacserv's terse PV+flags — stack diversity on the wire; natural drift cadence ≈1–2 s per notification. Still missing: TTL-expiry ghost test (5-min job on the isolated net). |
| `lab6-device-death.pcapng` | **The inverse ghost (2026-06-10, late).** Intended as the TTL-expiry test; instead captured the DEVICE dying seconds after ACKing two fresh subscriptions — then 218 s of silence with **zero ICMP** (the sim never attempted another send) and no client renewals (both apps closed). | Confirms the reference hierarchy: in lab1, bacserv answered FROM 47808 (port-anchored, certified-gear behavior) while the .NET family (YABE client + Room Sim) sources from ephemeral sockets — James's "YABE is B.S.-ing the COV" verdict, upheld. **Rig doctrine: bacserv = ground-truth device; Room Sim = convenience tool, its port habits are not reference behavior.** TTL ghost test STILL pending: needs device alive + drifting, CLIENT killed (next session, 5 min). |
| `captureforclaude1.pcapng` / `cp2.pcapng` | Earlier exploratory captures (pre-rig). | — |
| `dgfwegw.csv` | Exploratory export. | — |
